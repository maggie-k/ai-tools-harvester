{
  "title": "AI Tools Harvester input",
  "description": "Discover, score, and compare AI tools based on a specific use case.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Use case or task",
      "type": "string",
      "description": "Describe what you want to use AI for (e.g. content writing, meeting notes, customer support).",
      "editor": "textfield",
      "prefill": "content writing tools",
      "minLength": 3
    },
    "maxResults": {
      "sectionCaption": "Result settings",
      "title": "Maximum results",
      "type": "integer",
      "description": "Maximum number of AI tools to return.",
      "minimum": 1,
      "maximum": 20,
      "default": 7
    },
    "strictness": {
      "title": "Matching strictness",
      "type": "integer",
      "description": "Controls how strictly the input query must match tool descriptions (0 = loose, 100 = very strict).",
      "minimum": 0,
      "maximum": 100,
      "default": 59
    },
    "enableScraping": {
      "sectionCaption": "Scraping settings",
      "title": "Enable live scraping",
      "type": "boolean",
      "description": "If enabled, the Actor will scrape sources live. If disabled, it may use cached or static data.",
      "default": false
    },
    "sources": {
      "title": "Sources to include",
      "type": "array",
      "description": "Select AI tool directories to include.",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["aitooldirectories"]
      },
      "default": ["aitooldirectories"],
      "minItems": 1
    }
  },
  "required": ["query"]
}
